{
  "name": "manage-orders",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "vercel-build": "npm run build"
  }
}
